{
  "name": "figma-bridge-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build:plugin": "tsdown --config figma-plugin/tsdown.config.ts",
    "watch:plugin": "tsdown --config figma-plugin/tsdown.config.ts --watch",
    "typecheck:plugin": "tsc -p figma-plugin/tsconfig.json",
    "dev:bridge": "cd mcp-bridge && npm run build && node dist/index.js",
    "dev": "concurrently --names \"plugin,bridge\" --prefix-colors \"cyan,green\" \"npm run watch:plugin\" \"npm run dev:bridge\""
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.129.0",
    "concurrently": "^10.0.3",
    "tsdown": "^0.22.3",
    "typescript": "^6.0.3"
  }
}
