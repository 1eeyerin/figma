# 프론트엔드 설계 가이드라인

코드 작성·리뷰 시 4가지 축을 따른다.

## 가독성

- 매직 넘버는 의미 있는 상수로 (`ANIMATION_DELAY_MS = 300`).
- 복잡한 로직·상호작용은 전용 컴포넌트/HOC로 추상화 (예: 인증은 `AuthGuard`).
- 역할·상태별로 크게 다른 UI는 별도 컴포넌트로 분리.
- 중첩 삼항은 `if/else`나 IIFE로.
- 단순·국소 로직은 한곳에 모아 위→아래로 읽히게 (인라인 `switch`, 근처 정책 객체).
- 복잡·재사용·테스트 대상 조건은 이름 있는 변수로 (단순 1회성은 예외).

## 예측 가능성

- 비슷한 함수·훅은 반환 타입 통일 (API 훅은 Query 객체, 검증 함수는 Discriminated Union).
- 함수는 이름이 드러내는 동작만 (숨은 부작용 금지, 단일 책임).
- 표준 API 래퍼는 모호하지 않은 고유한 이름으로.

## 응집도

- 폼은 필드 단위/폼 단위 응집도를 의도적으로 선택.
- 디렉토리는 코드 종류가 아닌 기능·도메인 기준으로.
- 상수는 관련 로직 근처에 두거나 이름으로 관계를 명확히.

## 결합도

- 갈라질 수 있는 중복은 섣불리 추상화하지 않는다 (약간의 중복 > 잘못된 결합).
- 거대한 훅·컨텍스트는 작고 집중된 단위로 분할.
- Props Drilling 대신 컴포넌트 합성 사용.
